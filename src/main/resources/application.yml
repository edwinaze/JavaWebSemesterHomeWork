spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/javaweb
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #开启SQL语句执行日志信息
    show-sql: true
    hibernate:
      #配置为检查数据库表结构，没有时会自动创建
      ddl-auto: update

